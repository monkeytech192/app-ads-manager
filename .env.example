# =====================================================
# CẤU HÌNH ỨNG DỤNG ADS MANAGER
# =====================================================
# 
# 📝 FILE NÀY LÀ MẪU (template) - Không commit giá trị thật!
#
# 🔧 SỬ DỤNG:
# - Development local: Copy thành .env và điền giá trị thật
# - Production (Railway/Vercel): Nhập biến vào Environment Variables trên Dashboard
#   → KHÔNG CẦN file .env trong production!
#
# =====================================================

# --------------------- FRONTEND (Client) ---------------------
# API URL - URL của backend server
# Local dev: http://localhost:5000/api/v1
# Production: https://your-app-production.up.railway.app/api/v1
VITE_API_URL=https://your-railway-domain.up.railway.app/api/v1

# Facebook App ID (BẮT BUỘC)
# Lấy từ: https://developers.facebook.com/apps
VITE_FB_APP_ID=your_facebook_app_id

# Facebook Config ID (TÙY CHỌN - chỉ cần nếu dùng Business Integration)
# Để trống nếu chỉ login cá nhân
VITE_FB_CONFIG_ID=

# Facebook Access Token (BẮT BUỘC - cho ads data)
# Access token dài hạn để truy xuất dữ liệu quảng cáo
# Lấy từ: https://developers.facebook.com/tools/explorer/
# Chọn app → Get User Access Token → Chọn permissions: ads_read, ads_management
# Sau đó extend token: https://developers.facebook.com/docs/facebook-login/guides/access-tokens/get-long-lived
VITE_FB_ACCESS_TOKEN=your_long_lived_facebook_access_token

# --------------------- BACKEND (Server) ----------------------
# MongoDB Connection String (BẮT BUỘC)
# MongoDB Atlas: mongodb+srv://user:pass@cluster.mongodb.net/dbname
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/ads-manager

# JWT Secret (BẮT BUỘC - tạo random string 32+ ký tự)
# Tạo bằng: openssl rand -base64 32
JWT_SECRET=your_random_secret_minimum_32_characters

# Facebook App Secret (BẮT BUỘC - chỉ cho server)
# Lấy từ: https://developers.facebook.com/apps > Settings > Basic
FACEBOOK_APP_SECRET=your_facebook_app_secret

# Facebook Access Token (BẮT BUỘC - cho ads data)
# Access token dài hạn để truy xuất dữ liệu quảng cáo
# Lấy từ: https://developers.facebook.com/tools/explorer/
# Chọn app → Get User Access Token → Chọn permissions: ads_read, ads_management, business_management
# Sau đó extend token: https://developers.facebook.com/docs/facebook-login/guides/access-tokens/get-long-lived
FACEBOOK_ACCESS_TOKEN=your_long_lived_facebook_access_token

# Google Gemini API (TÙY CHỌN - để trống nếu không dùng AI)
GEMINI_API_KEY=

# =====================================================
# BIẾN TỰ ĐỘNG (Không cần config)
# =====================================================
# Các biến sau được platform tự động set, KHÔNG cần thêm:
# - NODE_ENV (Railway/Vercel tự set = "production")
# - PORT (Railway/Vercel tự assign)
# - CORS_ORIGIN (Backend tự động detect từ request)
# - JWT_EXPIRE (Mặc định 7 ngày)
# - FACEBOOK_APP_ID (Backend copy từ VITE_FB_APP_ID)

# =====================================================
# LƯU Ý QUAN TRỌNG VỀ ACCESS TOKEN
# =====================================================
# 
# App sử dụng 2 LOẠI TOKEN RIÊNG BIỆT:
#
# 1. LOGIN TOKEN (từ Facebook Login SDK):
#    - Dùng CHỈ cho đăng nhập/authentication user
#    - Token ngắn hạn (1-2 giờ)
#    - Được tạo tự động khi user click "Đăng nhập bằng Facebook"
#    - KHÔNG dùng để truy xuất dữ liệu quảng cáo
#
# 2. FACEBOOK_ACCESS_TOKEN (từ .env):
#    - Dùng CHỈ cho truy xuất dữ liệu quảng cáo (campaigns, metrics, etc.)
#    - Token dài hạn (60 ngày)
#    - BẮT BUỘC phải cấu hình trong .env
#    - Lấy từ: https://developers.facebook.com/tools/explorer/
#
# Lý do tách biệt:
# - Bảo mật: Access token có quyền cao không nên expose qua browser
# - Ổn định: Token dài hạn không bị expired giữa chừng
# - Linh hoạt: Admin có thể control token riêng cho từng deployment
#
# =====================================================

# =====================================================
# HƯỚNG DẪN DEPLOY
# =====================================================
# 📖 Chi tiết: Xem file DEPLOY.md
#
# Railway/Vercel Environment Variables:
# 1. Vào Dashboard → Settings → Environment Variables
# 2. Thêm TỪNG BIẾN ở trên (5 biến BẮT BUỘC)
# 3. Save → Auto redeploy
#
# KHÔNG CẦN:
# - ❌ File .env.production trong repo
# - ❌ Commit file .env lên Git (đã có trong .gitignore)
# =====================================================

